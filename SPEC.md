# 開発会社物語 — 仕様

> タイトルは「開発会社物語」。カイロソフトの「〜物語」様式へのオマージュ。
> 元ネタは README で名指しする（借りるのは仕組みと様式まで、という過去作からの方針）。

---

## 1. なぜ作るか（過去2本の失敗から）

| 作ったもの | 増える・育つ | 判断 | 結果 |
|---|---|---|---|
| そだつ / コミット（放置ゲー） | ✅ | ❌ | 「面白みがない」 |
| みつもり（PMシム） | ❌ | ✅ | 「焦らない・迷わない」 |
| **これ** | ✅ | ✅ | ← 両方を入れる |

**みつもりのメンバーは消耗するだけで一切成長しなかった。** 判断だけあっても足りない。
カイロソフト系は「増える・育つ」と「判断」の両方がある。想定するのは **ゲーム発展国** の骨格。

## 2. 進め方を前2回から変える

前2回は「純粋関数 → シミュレータ → UI」の順で作り、どちらも**数字は正しいのに面白くなかった**。

カイロ系の面白さは数字ではなく**手触り**（増える・飛ぶ・育つ）にある。
シミュレータでは測れない領域なので、今回は順番を変える。

> **最小の動くループを先に画面で作り、「見ているだけで楽しいか」を確かめる。バランスは後。**

ここが死んでいたら、その先を作っても意味がない。

## 3. 段階1でやること

**案件を1本つくって、リリースして、評価が出る。** ここまで。

```
ジャンルを選ぶ → 技術を選ぶ → 社員を割り当てる
      ↓
開発がひとりでに進む（数字がポンポン飛ぶ）★ここが本体
      ↓
完成 → 4人のレビュアーが点をつける → 結果
```

段階2以降（今回は作らない）: 社員の成長・資金・雇用・複数案件・年表

## 4. 面白さの芯

### ① 数字が飛ぶ

開発中、社員が数十回にわたって小さな数字を出し続ける。

```
    田中  +4 技術力
         佐藤  +3 デザイン
  鈴木  +5 安定性
```

**これが段階1の主役。** 見ているだけで気持ちいいかを確かめるために作る。

### ② 隠された組み合わせ

```
ジャンル（業務システム / ECサイト / スマホアプリ）
   ×
技術（PHP / Java / Python / TypeScript）
```

相性表は**プレイヤーに見せない**。当たりを引くと数字が大きく跳ねる。
結果画面で「この組み合わせは相性が良かった」とだけ示唆し、**次に活きる知識**にする。

これがリプレイの動機になる。ゲーム発展国の「ジャンル×題材」と同じ仕掛け。

### ③ 4つの指標

| 指標 | 上げる人 |
|---|---|
| 使いやすさ | 設計が得意な人 |
| 技術力 | 実装が得意な人 |
| デザイン | デザインが得意な人 |
| 安定性 | 几帳面な人 |

社員ごとに得意がばらけているので、**誰を入れるかで仕上がりが変わる**。

## 5. 評価

開発が終わると4人のレビュアーが 1〜10点をつける（40点満点）。

```
  レビュアーA  8点  「使いやすい」
  レビュアーB  6点  「よくある感じ」
  ...
  合計 27点 → 売上 1,200万円
```

満点は出にくくする。**次はもっと上を狙える**と思わせるのが目的。

## 6. 設計原則（前2本から引き継ぐ）

1. ルールは `src/dev.js` の純粋関数だけ。DOM も `Date.now()` も触らない
2. 乱数は決定的（種から生成）。リロードでやり直せると学習が成立しない
3. テストを必ずセットで書く（`node:test`、依存パッケージなし）
4. コメントは日本語。**なぜそう決めたか**を残す

## 7. 段階1の完了条件

- ブラウザで1本つくって、評価まで出せる
- **数字が飛ぶのを見ていて楽しい**（ここが本番）
- 同じ種なら同じ結果になる

判定は正人さんが実際に触って行う。数字では測れない。
